# 🌦️ Dockerized Weather App

A simple weather web app built with Flask and Docker that fetches live weather data using the OpenWeather API.

## 🚀 How to Run Locally

```bash
docker build -t weatherapp .
docker run -p 5000:5000 weatherapp
